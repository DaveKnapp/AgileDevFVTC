@model IEnumerable<T5.Brothership.Entities.Models.User>

@{
    ViewBag.Title = "SearchResults";
    Layout = "~/Views/Shared/Search.cshtml";
}

<h1 class="text-center">User Search</h1>

<p>
    @using (Html.BeginForm("SearchResults", "Search", FormMethod.Get, new { search = "User" }))
    {
       @Html.ActionLink("Games", "GameSearchResults", new {search=Request.Params["Search"]})
       <div class="row">
        <div class="col-12 d-block text-center">
            @Html.TextBox("Search")
        </div>
       </div>
       <div class="row">
        <div class="col-12 d-block text-center">
            <input type="submit" class="btn btn-lg btn-primary" value="Search" />
        </div>
       </div>
    }
</p>

<div class="container">
    <div class="row">
        <div class="col-12">
             @*@Html.DisplayNameFor(model => model.UserName)*@
        </div>       
        <div>
            @*@Html.DisplayNameFor(model => model.ProfileImagePath)*@
        </div>       
    </div>
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-3 col-sm-6">
                <div class="panel panel-default portfolio-item">
                    <img src="@item.ProfileImagePath" alt="" class="img-responsive panel-body d-block">
                    <h4 class="panel-heading title-container text-center">
                        @Html.ActionLink(item.UserName, "User", "User", new { userName = item.UserName }, null)
                    </h4>
                </div>
            </div>
        }
    </div>
</div>
